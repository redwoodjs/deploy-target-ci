{
  "environments": [
    {
      "id": "development",
      "name": "Development",
      "region": "us-east-1",
      "source": {
        "branch": "main"
      },
      "services": [
        {
          "id": "redwood-api",
          "name": "Redwood API",
          "type": "fargate",
          "cpu": 0.25,
          "memory": 0.5,
          "port": 8911,
          "installCommand": "NODE_ENV=development yarn install --immutable",
          "buildCommand": "cd flightcontrol && yarn rw deploy flightcontrol",
          "startCommand": "yarn rw deploy flightcontrol --serve",
          "postBuildCommand": "echo 0",
          "healthCheckPath": "/graphql/health"
        },
        {
          "id": "redwood-web",
          "name": "Redwood Web",
          "type": "static",
          "singlePageApp": true,
          "installCommand": "NODE_ENV=development yarn install --immutable",
          "buildCommand": "cd flightcontrol && yarn rw deploy flightcontrol web",
          "outputDirectory": "web/dist"
        }
      ]
    }
  ]
}
